# Hero / product images

Real Arabeska transparent-background cutout PNGs (provided by the owner):

| File         | Used in                       |
|--------------|-------------------------------|
| `hookah.png` | Hero (left) + SIGNATURE float |
| `burger.png` | Hero (center, "FOOD")         |
| `mojito.png` | Hero (right, "DRINKS")        |

To swap any of these, replace the file with another transparent PNG of the same
name — the layout and `filter: drop-shadow()` styling pick it up automatically.
